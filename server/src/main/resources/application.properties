# MySQL DataSource properties
spring.datasource.url=jdbc:mysql://46.101.133.139:3306/student_queue?useSSL=false&serverTimezone=UTC
spring.datasource.username=queue
spring.datasource.password=queue
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Hibernate auto means it will update schema database, so we dont need manually manage schema and entities.
## we do not have any ssl for this project
